# Traffic Catcher 이메일 OTP 관리자 인증 기획서

## 1. 목적

Traffic Catcher의 시스템 지침 관리자 기능을 기존 비밀번호 입력 방식에서 **등록된 관리자 이메일과 6자리 OTP 인증 방식**으로 개선한다.

관리자는 등록된 이메일로 일회용 인증번호를 받아 인증하고, 제한된 시간 동안 관리자 모드에서 다음 기능을 사용할 수 있다.

- 키워드 글용 AI 시스템 지침 조회 및 수정
- 메모·스토리 글용 AI 시스템 지침 조회 및 수정
- 시스템 지침 수정 이력 확인
- 관리자 이메일 추가 및 비활성화
- 관리자 로그인 및 중요 작업 감사 기록 확인

## 2. 현재 구조와 개선 필요성

현재 로컬 서버는 `X-Admin-Password` 헤더로 관리자 비밀번호를 확인한다. 시스템 지침은 프로젝트의 Markdown 파일에 저장되며, Cloudflare Pages에서는 정적 파일을 실행 중에 직접 수정할 수 없기 때문에 클라우드 관리자 화면의 저장 기능이 제한된다.

OTP 인증을 Cloudflare에서도 정상적으로 사용하려면 인증 기능뿐 아니라 서버 측 세션 처리와 시스템 지침용 영구 저장소가 필요하다.

## 3. 권장 시스템 구성

```text
관리자 이메일 입력
        ↓
Cloudflare Pages Function
        ↓
등록된 관리자 이메일 확인
        ↓
6자리 OTP 생성 → 해시값 임시 저장
        ↓
등록된 이메일로 인증번호 발송
        ↓
사용자가 6자리 인증번호 입력
        ↓
OTP 검증 및 즉시 폐기
        ↓
보안 관리자 세션 쿠키 발급
        ↓
시스템 지침 조회·편집·저장 허용
```

### Cloudflare 구성 요소

- **Pages Functions**: OTP 요청, 검증, 세션 확인, 관리자 API 처리
- **D1 데이터베이스**: 관리자 목록, 시스템 지침, 버전 이력, 감사 기록 저장
- **Workers KV**: OTP, 재전송 제한, 임시 세션처럼 만료가 필요한 데이터 저장
- **Cloudflare Secrets**: 세션 서명키, 이메일 발송 정보, 최초 관리자 이메일 저장
- **Email Service 또는 외부 메일 서비스**: OTP 이메일 발송
- **Turnstile**: 자동화된 OTP 요청과 무차별 대입 공격 방지

## 4. 데이터 저장 설계

### 4.1 D1 테이블

#### `admin_users`

| 필드 | 설명 |
|---|---|
| `id` | 관리자 식별자 |
| `email` | 정규화한 관리자 이메일 |
| `status` | `active`, `disabled` |
| `created_at` | 등록 시각 |
| `last_login_at` | 최근 인증 시각 |

#### `system_instructions`

| 필드 | 설명 |
|---|---|
| `instruction_type` | `keyword`, `story` |
| `content` | 현재 시스템 지침 본문 |
| `version` | 지침 버전 |
| `updated_by` | 수정한 관리자 |
| `updated_at` | 수정 시각 |

#### `system_instruction_history`

수정 전 내용을 버전별로 보관하여 이전 버전 비교 및 복원이 가능하게 한다.

#### `admin_audit_logs`

OTP 요청, 로그인 성공·실패, 지침 조회·수정, 관리자 변경 등 중요 작업을 기록한다.

### 4.2 KV 데이터

- OTP 해시값
- OTP 만료 시각
- 검증 실패 횟수
- 이메일·IP별 재발송 제한
- 관리자 세션 또는 세션 폐기 정보

OTP 원문은 저장하지 않고 해시값만 저장한다.

## 5. 인증 흐름

### 5.1 인증번호 요청

1. 관리자가 이메일을 입력한다.
2. 브라우저에서 Turnstile 토큰을 발급받는다.
3. 서버가 Turnstile 토큰을 검증한다.
4. 서버가 등록된 관리자 이메일인지 확인한다.
5. 암호학적으로 안전한 방식으로 6자리 OTP를 생성한다.
6. OTP 해시와 만료 정보를 KV에 저장한다.
7. 등록된 이메일로 OTP를 발송한다.
8. 이메일 등록 여부와 관계없이 화면에는 동일한 안내를 표시한다.

### 5.2 OTP 검증

1. 관리자가 이메일로 받은 6자리 번호를 입력한다.
2. 서버가 OTP 만료 여부와 실패 횟수를 확인한다.
3. 입력값을 해시하여 저장된 값과 안전하게 비교한다.
4. 인증 성공 시 OTP를 즉시 폐기한다.
5. 관리자 세션을 만들고 보안 쿠키를 발급한다.

### 5.3 관리자 세션

- 기본 유효시간: 60분
- 중요 작업 시 세션 만료 여부 재검증
- 로그아웃 시 즉시 폐기
- 쿠키 속성: `HttpOnly`, `Secure`, `SameSite=Strict`
- 세션 만료 5분 전 화면 안내

## 6. 보안 정책

| 항목 | 권장값 |
|---|---|
| OTP 길이 | 숫자 6자리 |
| OTP 유효시간 | 5분 |
| 최대 검증 실패 | 5회 |
| 재발송 대기시간 | 60초 |
| 이메일별 요청 제한 | 시간당 5회 |
| IP별 요청 제한 | 시간당 정책 적용 |
| 관리자 세션 | 30~60분 |

추가 보안 요구사항은 다음과 같다.

- OTP 생성에 `Math.random()`을 사용하지 않는다.
- 관리자 이메일을 프런트엔드 소스에 포함하지 않는다.
- 등록되지 않은 이메일에도 동일한 응답을 반환하여 이메일 목록 유출을 막는다.
- Turnstile 토큰은 서버에서 반드시 검증한다.
- 상태 변경 요청에 CSRF 방어를 적용한다.
- 시스템 지침 저장 전 길이와 허용 유형을 검사한다.
- 로그인과 지침 변경 내역을 감사 로그로 남긴다.
- 마지막 활성 관리자 계정은 삭제할 수 없게 한다.
- 비밀키와 이메일 서비스 인증정보를 GitHub에 커밋하지 않는다.

## 7. 관리자 이메일 등록 정책

최초 관리자 이메일을 공개 웹 화면에서 누구나 등록하게 해서는 안 된다. 공격자가 먼저 자신의 이메일을 등록할 수 있기 때문이다.

권장 등록 방식은 다음과 같다.

1. 최초 관리자 이메일을 Cloudflare Secret 또는 D1에 수동 등록한다.
2. 인증된 관리자만 추가 관리자 이메일을 등록할 수 있다.
3. 관리자 추가·비활성화 시 기존 관리자에게 알림을 발송한다.
4. 이메일 변경과 권한 변경 내역을 감사 로그에 기록한다.

## 8. 관리자 화면 구성

### 인증 전

- 관리자 이메일 입력
- `인증번호 보내기` 버튼
- Turnstile 영역

### 인증번호 발송 후

- 6자리 인증번호 입력
- `관리자 인증` 버튼
- 남은 유효시간 표시
- 재발송 가능 시각 표시

### 인증 완료 후

- 키워드 기사 지침 탭
- 내 스토리 기사 지침 탭
- 수정 내용 저장
- 현재 버전과 수정 시각
- 이전 버전 및 복원
- 관리자 로그아웃

상태 문구 예시는 다음과 같다.

- `🔒 관리자 인증 필요`
- `📧 인증번호 발송 완료 · 04:59`
- `✅ 관리자 인증 완료 · 52분 후 만료`
- `⚠️ 인증번호가 만료되었습니다. 다시 발송해 주세요.`

## 9. 시스템 지침 저장 및 적용

Cloudflare에서는 D1에 저장된 시스템 지침을 우선 사용한다.

```text
D1 시스템 지침 조회 성공
        ↓
AI API 요청에 즉시 적용

D1 접근 실패 또는 데이터 없음
        ↓
배포된 Markdown 기본 지침 사용
```

프로젝트의 기존 Markdown 파일은 초기 데이터와 장애 복구용 기본값으로 유지한다. 관리자가 D1에 저장한 지침은 GitHub를 다시 배포하지 않아도 다음 기사 작성부터 적용한다.

## 10. 로컬 서버 연동

- 로컬 웹 화면도 동일한 Cloudflare OTP 인증 API를 사용할 수 있다.
- 인터넷 연결이 없을 때를 대비해 기존 로컬 비밀번호 방식을 비상 관리 수단으로 선택적으로 유지할 수 있다.
- 비상 비밀번호는 환경변수 또는 안전하게 해시된 로컬 설정으로만 관리한다.
- 로컬과 클라우드의 시스템 지침 원본이 달라지지 않도록 D1을 우선 원본으로 사용한다.

## 11. 권장 API

| API | 역할 |
|---|---|
| `POST /api/admin/auth/request-otp` | OTP 요청 |
| `POST /api/admin/auth/verify-otp` | OTP 검증 및 세션 발급 |
| `POST /api/admin/auth/logout` | 관리자 로그아웃 |
| `GET /api/admin/session` | 현재 세션 상태 확인 |
| `GET /api/admin/system-instructions` | 시스템 지침 조회 |
| `PUT /api/admin/system-instructions/:type` | 시스템 지침 저장 |
| `GET /api/admin/system-instructions/:type/history` | 변경 이력 조회 |
| `POST /api/admin/system-instructions/:type/restore` | 이전 버전 복원 |
| `GET /api/admin/users` | 관리자 목록 조회 |
| `POST /api/admin/users` | 관리자 추가 |
| `PATCH /api/admin/users/:id` | 관리자 상태 변경 |

## 12. 개발 단계

### 1단계: 서버 기반 구축

- Pages Functions 추가
- D1 및 KV 생성과 바인딩
- Secret 등록
- 기본 관리자 이메일 등록

### 2단계: OTP 인증

- OTP 요청·발송·검증
- Turnstile 검증
- 요청 횟수 제한
- 보안 세션 쿠키

### 3단계: 시스템 지침 이전

- Markdown 기본값을 D1 초기 데이터로 등록
- 키워드·스토리 지침 조회 및 저장 API
- AI API 요청 시 D1 지침 우선 적용

### 4단계: 관리자 UI

- 이메일 및 OTP 인증 화면
- 세션 상태 표시
- 넓은 지침 편집 화면
- 버전 이력과 복원 기능

### 5단계: 검증 및 운영

- OTP 만료·재사용·오입력 테스트
- 이메일 및 IP 요청 제한 테스트
- 세션·CSRF·권한 우회 테스트
- 로컬 및 Cloudflare 환경 통합 테스트
- 감사 로그 및 장애 복구 테스트

## 13. 결론

등록된 관리자 이메일로 6자리 OTP를 발송하고 인증 후 관리자 모드에 진입하는 방식은 구현 가능하다. Traffic Catcher에서는 단순한 OTP 화면 추가가 아니라 **Pages Functions, D1/KV, 이메일 발송, 서버 세션, 시스템 지침 버전 관리**를 함께 구성해야 Cloudflare에서도 안전하고 완전한 관리자 기능으로 동작한다.

