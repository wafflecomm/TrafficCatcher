name = "trafficcatcher-scheduler"
main = "cloudflare-scheduler/worker.js"
compatibility_date = "2026-04-01"
workers_dev = true

[vars]
GITHUB_OWNER = "wafflecomm"
GITHUB_REPOSITORY = "TrafficCatcher"
GITHUB_REF = "main"

[triggers]
# Cloudflare Cron Trigger는 UTC 기준입니다.
# 0,30 0-14,21-23 * * * = KST 06:00~23:30 매 30분
# 0 6,13,22 * * * = KST 매일 07:00·15:00·22:00
crons = ["0,30 0-14,21-23 * * *", "0 6,13,22 * * *"]

[observability]
enabled = true
